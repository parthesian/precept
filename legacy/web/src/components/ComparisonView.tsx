export function ComparisonView() {
  return (
    <section className="panel">
      <h3 className="section-title" style={{ marginTop: 0, fontSize: "var(--text-xl)" }}>
        Similar Shots
      </h3>
      <p className="section-subtitle">
        Side-by-side shot strips and tag overlap highlights will render here.
      </p>
    </section>
  );
}
