# Retired for the live Precept app path (Workers + local D1).
# Kept as a no-op stub so older docs/scripts fail loudly instead of
# silently starting Postgres. Delete this file when convenient.
services: {}
