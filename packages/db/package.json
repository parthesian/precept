{
  "name": "@cinegraph/db",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./dist/queries.js",
  "types": "./dist/queries.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for @cinegraph/db\"",
    "db:migrate": "echo \"Run wrangler d1 migrations apply from apps/api\""
  },
  "dependencies": {
    "@cinegraph/shared": "file:../shared"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250320.0"
  }
}
