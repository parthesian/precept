{
  "name": "cinegraph",
  "private": true,
  "packageManager": "npm@11.6.2",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:api": "turbo dev --filter=@cinegraph/api",
    "dev:web": "turbo dev --filter=@cinegraph/web",
    "build": "turbo build",
    "pipeline": "turbo pipeline --filter=@cinegraph/pipeline",
    "db:migrate": "turbo db:migrate --filter=@cinegraph/db",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint"
  },
  "devDependencies": {
    "turbo": "^2",
    "typescript": "^5.5"
  }
}
