/**
 * Package entry for tests and tooling. The deployable Worker mounts `createApp()`
 * from `apps/web/workers/app.ts`.
 */
export { createApp } from "./create-app.js";
export type { ApiBindings } from "./env.js";
